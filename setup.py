from setuptools import setup

setup(name='conch',
      version='0.0.1',
      author='Tristan Heywood',
      packages=['conch'],
      description='Utilities for assisting triton kernel development.',
      license='MIT',
      install_requires=[
      ],
)
